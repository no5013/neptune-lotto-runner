{
  "name": "neptune-lottery",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "// draw":  "Full pipeline: lottery → publish to web → export Excel",
    "draw":     "tsx scripts/orchestrate.ts run",
    "// dev":   "Start the Next.js dev server",
    "dev":      "npm run dev --prefix lottery-result",
    "// build": "Build the Next.js app for production",
    "build":    "npm run build --prefix lottery-result"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/papaparse": "^5.5.2",
    "exceljs": "^4.4.0",
    "papaparse": "^5.5.4",
    "tsx": "^4.23.0",
    "typescript": "^5"
  }
}
